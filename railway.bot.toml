# Use this config for the clawcfo-bot Railway service (Settings → Config file path).
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.bot"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
